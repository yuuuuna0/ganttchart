<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weaverloft.ganttchart.mapper.BoardMapper">
    <!--1. 게시글 쓰기-->
    <insert id="createBoard" parameterType="java.util.Map">
        insert into board(board_no, board_title, id, board_date, board_content)
        values (#{boardNo},#{boardTitle},#{id},sysdate,#{boardContent})
    </insert>
    <!--2. 게시글 전체 리스트 보기 ==> 페이징처리 필요 -->
    <select id="selectBoardList" resultType="com.weaverloft.ganttchart.dto.Board">
        select * from board
    </select>
    <!-- 3. 게시글 상세보기 -->
    <select id="selectByBoardNo" parameterType="java.lang.Integer" resultType="com.weaverloft.ganttchart.dto.Board">
        select * from BOARD where BOARD_NO=#{boardNo}
    </select>
    <!-- 4. 게시글 삭제 -->
    <delete id="deleteBoard" parameterType="java.lang.Integer">
        delete BOARD where BOARD_NO=#{boardNo}
    </delete>
    <!-- 5. 게시글 수정 -->
    <update id="updateBoard" parameterType="java.util.Map">
        update BOARD set BOARD_TITLE=#{boardTitle}, BOARD_CONTENT=#{boardContent} where id=#{id}
    </update>




</mapper>